# URL du backend (le site web) auquel envoyer les events de loot.
# Pour tester en local contre `cd web && npm run dev` :
BACKEND_URL=http://localhost:3000

# Pour utiliser la vraie prod, commente la ligne au-dessus et décommente celle-ci :
# BACKEND_URL=https://pfmanager.vercel.app
